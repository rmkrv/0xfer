#!/bin/sh
set -eu

tmpdir="$(mktemp -d)"
trap 'rm -rf "$tmpdir"' EXIT

hcc2d_encoder --help >/dev/null
hcc2d_encoder --text "debian autopkgtest" --mode qr --file-format raw "$tmpdir/out.png"
test -s "$tmpdir/out.png"
