# Local build artifacts
hcc2d_encoder
*.o
SHA256SUMS.txt
release/
*.deb
*.build
*.buildinfo
*.changes
*.dsc
*.tar.*

# Debian packaging work files
debian/.debhelper/
debian/debhelper-build-stamp
debian/files
debian/hcc2d-encoder/
debian/*.debhelper.log
debian/*.substvars
debian/tmp/

# Temporary local output
dist/
apt-repo/tmp/

# Generated APT repository content
apt-repo/public/*

# Keep only the public archive key versioned
!apt-repo/public/hcc2d-archive-keyring.gpg

# Private signing material
.secrets/
