name: CLA

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize]

permissions:
  actions: write
  contents: write
  pull-requests: write
  statuses: write

jobs:
  cla:
    runs-on: ubuntu-latest
    steps:
      - name: CLA Assistant
        # contains() so a pasted "> " prefix or trailing period still triggers
        # the run; the action itself validates the signature text.
        if: (contains(github.event.comment.body, 'recheck') || contains(github.event.comment.body, 'I have read the CLA Document and I hereby sign the CLA')) || github.event_name == 'pull_request_target'
        # SHA-pinned (v2.6.1): third-party code running with a write-scoped
        # token in a pull_request_target context must be immutable.
        uses: contributor-assistant/github-action@ca4a40a7d1004f18d9960b404b97e5f30a505a08
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Signatures live in the decimen-optical-transfer repo (shared store),
          # so cross-repo writes need a PAT with contents:write on that repo.
          PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        with:
          path-to-signatures: signatures/cla.json
          path-to-document: https://github.com/bashalarmistalt/decimen-optical-transfer/blob/main/CLA.md
          branch: cla-signatures
          allowlist: bashalarmistalt
          remote-organization-name: bashalarmistalt
          remote-repository-name: decimen-optical-transfer
